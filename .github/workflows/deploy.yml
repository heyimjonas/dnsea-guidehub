# After (Fixed)
- name: Checkout Code
  uses: actions/checkout@v6  # Upgraded version

- name: Build Vite Vue App
  run: |
    npm install
    npm run build

- name: Upload Build Artifacts
  uses: actions/upload-artifact@v7  # Upgraded version
  with:
    name: vite-dist
    path: dist/
